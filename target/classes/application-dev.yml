spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/plataforma_conversacional_dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: DEBUG
    com.plataforma.conversacional: TRACE
